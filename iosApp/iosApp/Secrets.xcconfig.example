// Template: copy to Secrets.xcconfig (gitignored) and fill in your real key.
// GEMINI_API_KEY removed: Gemini is now called through an authenticated Cloud
// Function proxy (functions/src/geminiProxy.js), never directly from the app.
